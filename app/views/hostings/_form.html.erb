<%= form_for(@new_host) do |host| %>
  <% if @new_host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_host.errors.count, "error") %> prohibited this course from being saved:</h2>
      <ul>
      <%= @new_host.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form_view" id="hosting_form_view">
  <div class="form_labels" id="hosting_form_labels">
        <p><%= host.label :domain_name, :class => :form_label %><br />
        <p><%= host.label :hosting_package, :class => :form_label %><br />
        <p><%= host.label :domain_registration, :class => :form_label %><br />
        <p><%= host.label :email, :class => :form_label %><br />
        <p><%= host.collection_select(:customer_id, Customer.all, :id, :customer_name ) %><br />
        <p><%= host.label :contact_name, :class => :form_label %><br />
        <p><%= host.label :contact_email, :class => :form_label %><br />
        <p><%= host.label :additional_domains, :class => :form_label %><br />
        <p><%= host.label :notes, :class => :form_label %><br />
  </div><!-- form_labels -->

  <div class="form_fields" id="hosting_form_fields">
    <p class=""><%= host.text_field :domain_name %><br>
    <p class=""><%= host.text_field :hosting_package %><br>
    <p class=""><%= host.check_box :domain_registration %><br>
    <p class=""><%= host.check_box :email %><br>
    <p class=""><%= host.number_field :customer_id %><br>
    <p class=""><%= host.text_field :contact_name %><br>
    <p class=""><%= host.text_field :contact_email %><br>
    <p class=""><%= host.text_area :additional_domains %><br>
    <p class=""><%= host.text_area :notes %><br>
  </div><!-- form_fields -->
    <div class="actions">
      <%= f.submit %>
    </div>
  
    
</div><!-- form_view -->

<% end %>